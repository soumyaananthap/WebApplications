var age = prompt("What is your age?");
// alert("Your are alive for " + age * 365 + " days");
// include leap year every 4 years
var days = age * 365.25;
alert(age + " years is roughly " + days + " days")